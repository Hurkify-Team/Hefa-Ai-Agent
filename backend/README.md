# HEFAMAA Smart Registry Agent Backend

This folder is reserved for backend-specific services that should stay separate from the frontend workspace.

For the MVP, Next.js route handlers will live inside `frontend/app/api` as requested by the project brief. If the backend grows into standalone workers or services, they should be added here without mixing them into the UI code.
