# HEFAMAA Smart Registry Agent

Project layout:

```text
backend/
frontend/
```

The current UI implementation lives in `frontend/`. Backend-specific services will stay in `backend/`, while MVP Next.js API route handlers can live in `frontend/app/api`.
